{
  "name": "secureauth-spicedb-poc",
  "description": "small todo app that uses SecureAuth for login/identity and SpiceDB for relationship-based authorisation",
  "private": true,
  "packageManager": "pnpm@8.15.6",
  "devDependencies": {
    "turbo": "^2.9.6",
    "@biomejs/biome": "latest"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "check": "biome check .",
    "format": "biome check . --write"
  }
}
